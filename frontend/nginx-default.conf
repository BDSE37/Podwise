# Nginx 伺服器設定檔
# 本檔案由 AI 助手自動產生，請勿隨意刪除
# 主要用於靜態前端服務與健康檢查

server {
    listen 80;
    server_name localhost;

    # 靜態檔案服務
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # 健康檢查端點
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
} 